appName: myhelmapp

replicas: 1

# namespace: Webapp ## the deployment namespace is missing the s >> namepace!!

port: 80

configmap:
  name: myhelmapp-configmap-v1
  data:
    CUSTOM_HEADER: "This app was deployed with helm!"

image:
  name: nginx
  tag: latest
